{
  "name": "reconhecimento-por-gestos",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "electron-main.js",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview",
    "electron": "concurrently \"vite\" \"wait-on http://localhost:5173 && electron .\""
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "^0.10.35"
  },
  "devDependencies": {
    "concurrently": "^10.0.0",
    "electron": "^42.3.0",
    "vite": "^8.0.12",
    "wait-on": "^9.0.10"
  }
}